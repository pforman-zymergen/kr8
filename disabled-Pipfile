# This is only used for generating the docs
# It will need to be updated if we resume doing that
# (and maybe use docker instead of pipenv-ing all over my Python install...)
[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
mkdocs = "*"
mkdocs-material = "*"

[requires]
python_version = "2.7"
